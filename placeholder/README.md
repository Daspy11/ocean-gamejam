# placeholder/ — generated stand-ins

Everything here is produced by `npm run placeholders` (`scripts/placeholders.mjs`). Do not hand-edit.
Flat-colour silhouettes from a few rectangles, flat tiles, and `[PLACEHOLDER ...]` text only. Not final
art, not final writing.

To replace one: put the real file at the same relative path under `../assets/`. Delete nothing here.
